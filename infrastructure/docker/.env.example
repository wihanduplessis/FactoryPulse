# ---- SQL Server ----
# 8+ chars, with uppercase, lowercase, a digit and a symbol.
MSSQL_SA_PASSWORD=ChangeMe_Strong123!

# ---- JWT signing key ----
# MUST be at least 32 characters (HMAC-SHA256 requires a 256-bit key).
JWT_KEY=ChangeMe-this-must-be-32-chars-min!!

# ---- Seeded admin (this is the account you log in with) ----
# Password: 6+ chars with uppercase, lowercase, a digit and a symbol.
SEED_ADMIN_EMAIL=admin@factorypulse.local
SEED_ADMIN_PASSWORD=ChangeMe_Admin123!